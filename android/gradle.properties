org.gradle.jvmargs=-Xmx4G -XX:+UseG1GC
android.useAndroidX=true
android.enableJetifier=true
gradle.user.home=/home/user/.gradle
org.gradle.caching=true

# Enable 16KB page size support
# This ensures the app is compatible with devices using 16KB memory pages
# Required for Android 15+ (API 35+) on certain devices
# Note: Native library alignment is automatically handled by AGP 8.0+
# https://github.com/rive-app/rive-flutter/issues/539
rive.ndk.version=28.1.13356709
android.ndk.suppressMinSdkVersionError=21